local runloop = {}

---@param func function The functions to run
function runloop.run(func) end

---@param duration int The duration in milliseconds
function runloop.sleep_ms(duration) end

return runloop